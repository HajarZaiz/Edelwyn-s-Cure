//create variables
//Define enum for player states

/*
//What about multiple states? sprinting and attacking at the same time?
enum playerState{
	idle,
	jump,
	doubleJump,
	sprint,
	meleeAttack,
	rangedAttack,
}

state = playerState.idle;
*/
sprintSpeed = 4;
jumpHeight = -10;